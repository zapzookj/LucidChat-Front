# LucidChat-Front — 작업 지침

## UI/UX 작업 원칙 (2026-08-14 로비 재설계에서 확립 — 종원 승인)

이 원칙은 화면(UI)을 만들거나 고치는 모든 작업에 적용된다. 재설계 1차 반려의 원인이
"지시·와이어프레임의 수동적 이행"이었고, 아래 방식으로 전환한 뒤 결과물이 승인됐다.

1. **역할**: 시니어 UX/UI 디자이너로 행동한다. 최대 퀄리티가 목표다. 기존 스펙·와이어프레임·
   지시보다 나은 안이 있으면 **근거와 함께 제안하고 구현한다**. 수동적 이행 금지.
2. **와이어프레임·레퍼런스는 구조 힌트일 뿐 시각 스펙이 아니다.** 그대로 옮기지 않는다.
3. **디자인 아티팩트 먼저**: 화면 신규·개편은 인터랙티브 HTML 목업 제작 → 종원 컨펌 → 구현.
   텍스트 설명만으로 디자인을 추천하지 않는다(목업 없이 추천했다 번복한 전례 있음).
4. **디자인 크리틱 필수**: 목업과 구현 각각에 다관점 검수(초심자 이해도 / 시각 시스템·토큰 /
   한국어 카피 / 반응형·상태·접근성)를 돌려 확정 결함을 수정한다.
5. **항상 설계할 것**: 디자인 토큰(타입 스케일·간격 리듬·대비 규칙) · 로딩/빈/오류 상태 ·
   게스트/멤버 분기 · 데스크톱+모바일 양쪽(데스크톱 제1 캔버스) · 콘텐츠 가변성(긴 이름·0개·
   부분 실패) · 한국어 카피(조사 와/과 처리 = `src/utils/josa.js`, 어미 톤 통일, 기능어 1순위 +
   감성 1겹).

## 로비(블록 A) 정본

- 디자인 정본: `aichat` 저장소 `docs/15_assets/lobby_redesign_mockup.html` — 토큰·카피·상태
  매트릭스·브레이크포인트·구현 계약이 화면 '설계 문서'에 있다. 구현과 어긋나면 목업이 정본.
- 탭 4(기능형 확정): 홈 `/` · 스토리 `/story` · 스튜디오 `/studio` · 보관함 `/archive`.
- 게스트 게이트: 탐색 전부 열림, **행동 시점에만** 로그인 시트(GuestLoginGate). 버튼에
  자물쇠·비활성 표시 금지. 보관함·스튜디오 탭은 게스트 클릭 시 시트.
- 보존 로직(수정 금지 주의): `src/utils/postLogin.js` 딥링크 복원 전 체계,
  ChatPage/ChatPageV2 인트로 페이드 게이트, 사운드 정책(BGM 옵트인 · 호버/클릭 SFX 금지 ·
  결정적 순간만 `sfx.chime`).

## 스타일 규약

- Tailwind 단일 체계(tailwind.config.js의 `lobby.*` 색·`lb-*` 타입 토큰 사용). 인라인 style은
  그라데이션 등 토큰화 불가 값에만.
- framer-motion은 절제: 탭 전환 enter-only 1회, 카드 호버 변형 정도. 이중 페이드 금지.
- 모든 클릭 요소는 button 또는 href 있는 a(div+onClick 금지). 동적 Tailwind 클래스 합성 금지
  (JIT — 전체 클래스 문자열만).
